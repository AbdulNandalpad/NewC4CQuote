{
  "name": "NewC4CQuote",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "@cap-js/sqlite": "^3",
    "@sap/cds": "^10",
    "cors": "^2.8.6"
  },
  "devDependencies": {
    "@sap/cds-dk": "^10"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "private": true,
  "cds": {
    "features": {
      "in_memory_db": true
    },
    "requires": {
      "API6": {
        "kind": "rest",
        "credentials": {
          "destination": "API6"
        }
      },
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": ":memory:"
        }
      }
    }
  }
}
