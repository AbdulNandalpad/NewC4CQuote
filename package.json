{
  "name": "NewC4CQuote",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "@sap/cds": "^10",
    "@sap/xssec": "^4",
    "cors": "^2.8.6"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^3",
    "@sap/cds-dk": "^10"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "private": true,
  "cds": {
    "requires": {
      "API6": {
        "kind": "rest",
        "credentials": {
          "destination": "API6"
        }
      },
      "[production]": {
        "auth": "xsuaa"
      },
      "auth": {
        "users": {
          "alice": { "password": "alice", "roles": ["PricingUser"] },
          "bob": { "password": "bob", "roles": [] }
        }
      }
    }
  }
}
